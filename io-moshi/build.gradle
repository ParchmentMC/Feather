group = 'org.parchmentmc.feather'
archivesBaseName = 'io-moshi'

dependencies {
    api project(':feather')
    api 'com.squareup.moshi:moshi:1.12.0'
}

publishing {
    publications.create("moshiIO", MavenPublication) {
        from components.java
    }
}
